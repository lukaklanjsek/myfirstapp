[project]
name = "syncope"
version = "0.1.0"
description = "crm app for musicians"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asgiref>=3.11.0",
    "django==6.*",


    "gunicorn>=23.0.0",
    "packaging>=25.0",

    "python-dotenv>=1.2.1",
    "sqlparse>=0.5.5",
]
